import React from "react";
import Navigator from "../Components/Navigator";

const Landing = () => {
  return (
    <div>
      <Navigator />
      Landing
    </div>
  );
};

export default Landing;
